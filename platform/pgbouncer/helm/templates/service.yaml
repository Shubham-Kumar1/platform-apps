apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 6432
      name: pgbouncer
  selector:
    app: {{ .Release.Name }}
